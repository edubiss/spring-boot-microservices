spring:
  application:
    name: equipe-service
  cloud:
    config:
      uri: http://localhost:8088
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
    org:
      h2:
        Driver: dbc:h2:mem:db;DB_CLOSE_DELAY=-1 
